import React from 'react';

const Loading = () => {
    return (
        <main className="wrap">
            <div className="loading">
                <div>Loading...</div>
            </div>
        </main>
    );
};

export default Loading;